# Identity policy

This ADR is intentionally pending the first corpus-wide `issue2-identity-audit` artifact.

Do not fill measured counts or preference semantics from synthetic fixtures. The final policy must be derived from the pinned corpus artifact and reviewed against issue #2.
