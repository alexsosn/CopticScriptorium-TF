# Workflow cost note

The current issue #2 corpus audit is embedded in the legacy issue #1 semantic job, so every identity change also reruns CoNLL-U parity, metadata reconciliation, PAULA and TEI audits. This is intentionally left unchanged during the identity-policy measurement cycle; optimization belongs in a separate ticket so it cannot alter #2 evidence collection mid-review.
